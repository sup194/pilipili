<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pilipili.server.mapper.VideoCategoryMapper">

    <select id="listByVideoId" resultType="com.pilipili.server.dto.CategoryDto">
        SELECT
            c.id,
            c.parent,
            c.`name`,
            c.sort
        FROM video_category vc
        JOIN category c ON c.id = vc.category_id
        ${ew.customSqlSegment}
    </select>

    <select id="QueryVideoByCategoryID" resultType="com.pilipili.server.dto.VideoDto">
        select * from pilipili.video
                          left join pilipili.video_category c
                                    on video.id = c.video_id
        where category_id = #{category_id}

    </select>

    <select id="QueryCategoryById" resultType="com.pilipili.server.entity.Category">
        select name from pilipili.category
                          left join pilipili.video_category c
                                    on category.id = c.category_id
        where video_id = #{video_id}
    </select>
</mapper>
