<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pilipili.server.mapper.VideoMapper">


    <select id="selectHotStudyVideos" resultType="com.pilipili.server.dto.VideoDto">
        SELECT
            v.*,
            vp.play_volume
            
        FROM video v
        JOIN video_playback vp ON vp.video_id = v.id
        JOIN video_category vc ON v.id = vc.video_id
        JOIN category c ON c.id = vc.category_id
        ${ew.customSqlSegment}
    </select>
</mapper>
